import { GuildMember } from "discord.js";

export async function createGame(user: GuildMember, name: string, won: boolean, bet?: number, earned?: number, xp?: number): Promise<any> {

}

export async function fetchGame(id): Promise<unknown> {
    return;
}